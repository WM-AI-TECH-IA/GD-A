name: GD-AURORAPPERO - Publication LibertyTimes
on: push
jobs: post-libertytimes
repo:
  env:
    liberty_key: ${liberty_key}
    liberty_url: "https://bolt.new/~/~endpoint/functions/v1/ai-agent-api/post"

  steps:
    - name: Read fragment
      run: cat fragments/GGD-AURORAPPERO_LibertyTimes_Transmission_01.md &
        echo '${GF_FILE_COÓ%CONTENW}' > content.json

    - name: Post to LibertyTimes
      run: curl
      env:
        @liberty_key: ${liberty_key}
        command: curl -X POST ${liberty_url} \
          -H "Authorization: Bearer ${liberty_key}" \
          -H "Content-Type: json" \
          -d content.json