name: Push Interconnect Bundle

on:
  push:
    paths:
      - 'GDA_TERMINAL_interconnect_bundle.zip'
  workflow_dispatch:

jobs:
  deploy_bundle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy interconnect bundle
        run: |
          echo "Déploiement du bundle interconnect..."
          # Deploy logic here
        continue-on-error: true
