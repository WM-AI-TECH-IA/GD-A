name: GDA-bootstrap
on:
  schedule:
    - cron: '0 */1 * * *'

  push:

    branches: ["main"]

jobs: 
  bootstrap-monitor:
    run: |
      name: Synth auto - Verif forme dévia
      runs:
        sh: |
          echo "REMAP: GD-A CENTRAL"
          git status
          git config remote.urls
          git sub-module --selected-checks='heartbeat | dezyp |push_interconnect' . | xxx