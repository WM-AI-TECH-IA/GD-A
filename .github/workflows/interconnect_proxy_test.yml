name: Interconnect Proxy Test

on:
  push:
    paths:
      - 'api/**'
      - 'core/**'
  workflow_dispatch:

jobs:
  test_interconnect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Test interconnect proxy
        run: |
          echo "Testing interconnect proxy..."
          # Test logic here
        continue-on-error: true
