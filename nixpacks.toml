[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r gpt_server/requirements.txt"
]

[start]
cmd = "cd gpt_server && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
