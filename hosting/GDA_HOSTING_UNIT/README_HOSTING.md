# Guide D'Hébergement GD-AURORAPERO

```clone
# REQUISITS
```
- `flask`
- `requests`

```
curl -L /app
docker compose up gda
git clone https://github.com/WM-AI-TECH-IA/GD-A

# 1. Start les services
docker compose up
docker compose up gda_core

```pthon
# Rest une fois instance localement
(en dehors ou Render)
```
pip install -r query flask requests
docker compose up

```p
cdoc /app
python GDA_LAUNCHER.py
```

# 2. Port exposé :8080
- Pour accéder la Swagger UIG+
- Test via browser : http://localhost:8080/
- Endpoint technique: /metamorphe, /mutate-noy, /

# 3. Rapports
- Linux, Termux, Render, GCP
panel multiagent, automutable, connecté avec Supabase

