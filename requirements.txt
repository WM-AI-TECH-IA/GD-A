# Core dependencies for GD-A GPT Server (Railway deployment)
# Heavy dependencies (torch, whisper) moved to service-specific requirements
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Note: For ML features, install separately in specific services:
# openai-whisper, torch, numpy, ffmpeg-python, espeakng
